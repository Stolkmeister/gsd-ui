---
phase: 1
plan: 2
type: implementation
wave: 1
depends_on: [1.1]
files_modified:
  - migrations/005_add_indexes.sql
  - migrations/006_add_constraints.sql
requirements:
  - TF-01
must_haves:
  - B-tree indexes on frequently queried columns
  - Unique constraints on email, project slug
  - Check constraints on status transitions
---

# Plan 1.2: Indexes and Constraints

## Objective
Add performance indexes and data integrity constraints to the core tables.

## Tasks
1. Add B-tree indexes on tasks.status, tasks.assignee_id, tasks.project_id
2. Add unique constraint on users.email
3. Add composite index on (project_id, status) for filtered queries
4. Add check constraints for valid status and priority values
